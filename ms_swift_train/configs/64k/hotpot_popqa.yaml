base: ../base.yaml

output_dir: ${output_dir}/hotpot_popqa_64k
run_name: hotpot_popqa_64k

training:
  max_length: 65536

data:
  datasets:
    - ${data_dir}/pop_qa_64k/development.jsonl
    - ${data_dir}/hotpot_qa_64k/development.jsonl
