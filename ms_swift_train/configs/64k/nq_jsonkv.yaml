base: ../base.yaml

output_dir: ${output_dir}/nq_jsonkv_64k
run_name: nq_jsonkv_64k

training:
  max_length: 65536

data:
  datasets:
    - ${data_dir}/json_kv_64k/development.jsonl
    - ${data_dir}/nq_64k/development.jsonl
