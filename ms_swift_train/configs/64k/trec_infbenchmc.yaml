base: ../base.yaml

output_dir: ${output_dir}/trec_infbenchmc_64k
run_name: trec_infbenchmc_64k

training:
  max_length: 65536

data:
  datasets:
    - ${data_dir}/infinite_bench_mc_64k/development.jsonl
    - ${data_dir}/trec_coarse_64k/development.jsonl
